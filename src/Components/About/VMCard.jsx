const AboutCTA = ({ title, description }) => {
    return (
      <div class="vmcontainer">
        <h2>{title}</h2>
        <p>{description}</p>
      </div>
    );
  };
  
  export default AboutCTA;